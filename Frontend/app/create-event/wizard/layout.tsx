export const metadata = {
  title: "Create Event | EventStay",
  description: "Plan your premium event and booking.",
};

export default function WizardLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}
